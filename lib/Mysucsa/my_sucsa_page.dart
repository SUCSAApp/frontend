import 'package:flutter/material.dart';

class MySUCSAPage extends StatelessWidget {
  const MySUCSAPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '我的',
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
    );
  }
}
