import 'package:flutter/material.dart';

class ReimbursementApprovalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('报销审批')));
}
